import React from "react";

function App({ count }) {
  return (
    <div className="App">
      <header className="App-header">
        <h1>{count}</h1>
      </header>
    </div>
  );
}

export default App;
